# 2026-08-16 工作记录

## 希凡工作台（班主任一站式工作台）
- 交付物：`C:\Users\nqszx\WorkBuddy\2026-08-16-11-15-17\index.html`（单文件 HTML 应用）
- 技术：纯原生 HTML/CSS/JS，三套主题用 CSS 变量 + `data-theme`，数据全部存 `localStorage`（key `xifan_workbench_v1`），支持 JSON 导出/导入。
- 模块：工作待办、AI 智能助手（模板化文稿生成，可存话术库）、常用话术库（搜索/收藏/复制）、应急备忘、学生档案、座位网格、值日排班、家校联系、知识点、课程表、考勤、档案归档、成绩分析（试卷知识点得分率条形 + 完整成绩看板：均分/分布直方图 canvas/个体/分层/薄弱科/多班对比占位）。
- 修复的关键坑：模块按钮同时用了 `data-action` 与 `data-act`，主事件委托只认 `data-act` 导致大量按钮失效 → 改为 `el.dataset.act||el.dataset.action` 统一处理；话术删除 `del` 与待办 `del` 冲突 → 话术改用 `ph-del`；座位网格列数少算一列（标签列）→ 改为 `64px repeat(cols,1fr)`。
## 希凡工作台首页改版（第二轮）
- 用户参考 `/image#1`（半亩方塘工作台）要求首页重做成卡片式：欢迎横幅 + 实时时钟 + 4 快捷入口 + 每日一言 + 两栏（今日课程 / 提醒事项）。
- 顶部品牌改为「希凡工作台 + 班主任 & 教师工作台」副标题；右侧中部新增大字号日期 + 周徽章（橙色渐变）。
- 4 个快捷入口：📋班级事务（→家校联系）/ ➕添加待办（弹窗）/ 🔍应急备忘（→应急）/ 🤖AI助手（弹窗，含文稿生成 + 话术库 tab）。
- 原本首页的「工作待办/AI助手/话术库」全部下移到弹窗，避免首页冗长，同时通过提醒事项面板的「📋 全部」按钮打开完整待办管理。
- 修复：toggle/archive/del 之前只刷新旧的 `#todoList`，新建 `refreshTodoModal()` 同步刷新弹窗中的 `#todoMList` 与首页提醒；主事件委托新增 `data-go` 导航和 `todo-toggle` 处理。
- 实时时钟通过 `setInterval` 每秒更新，离开首页时定时器仍运行但因找不到 DOM 元素自动 no-op。

## 希凡工作台第三轮（侧边栏/座位/课程表/学生导入）
- 侧边导航改为完整文字标签：加宽到 112px，显示「图标 + 完整模块名」（班→班级首页，生→学生档案…），不再截断为 2 字。
- 座位管理：默认 8 行 8 列；HTML5 拖拽实现——座位间拖动互换、从未分配名单池拖入空位、拖回名单池即清空；保留点击弹窗分配；新增「自动排座」。左名单池 `.seat-pool` + 右侧 `8列` 网格 `34px repeat(8,1fr)`。
- 课程表：节次改为「第1-4节 + 午休 + 午餐 + 第5-8节 + 延时服务」共 11 行；单元格点击弹出**国家课程方案（山东初中）下拉选课**（`SHANDONG_COURSES` 常量 + `isSpecialPeriod` 区分午休/午餐/延时）；新增「填充示例课表」`schSample()` 生成典型山东初中周课表；首页「今日课程」跳过特殊节次；网格紧凑化（min-height 38px）尽量一页显示。`load()` 内加 `migrate()`：旧默认（6×7座位 / 6节次）自动升级为 8×8 / 11节次。
- 学生批量导入：学生档案新增「⬆ 批量导入」按钮 → 宽弹窗 `openStImport()`，四个来源：Excel/CSV（`xlsx` SheetJS）、图片OCR（Tesseract.js chi_sim）、Word（mammoth 仅 .docx）、文本粘贴；解析后表格预览可编辑/删除，支持「确认导入(追加)」或「替换全部」。CDN 库用 `loadScript()` 按需动态加载并 try/catch 优雅降级（离线核心不受影响）。
- 校验：`node --check` 内联 JS 通过；`data-action` 与 `data-act` 仍由 1640 行的统一监听 `el.dataset.act||el.dataset.action` 处理，已确认不与 451 行 handleAction 的顶栏动作重复。

## 希凡工作台第四轮（课程表加晨读）
- 节次首位新增「晨读」（共 12 行：晨读 + 第1-4节 + 午休 + 午餐 + 第5-8节 + 延时服务）。seed 默认 + `migrate()` 对「11节次但首格非晨读」的老数据前置晨读。
- 新增 `READING_COURSES` 常量（晨读·语文/英语/经典诵读/语文/英语…），编辑弹窗对 晨读 节次改用该下拉；`schSample()` 新增 `tmplReading` 按语数英轮换填充（周一/三/五 语文，周二/四 英语）。首页「今日课程」自然含晨读。

## 希凡工作台第五轮（课程表样式 + 跨设备云同步）
- 课程表单元格：改为 flex 居中布局，课程名/教师名垂直水平居中，字号 13px / 副文 11px；新增悬停提示气泡 `#schTip`（跟随光标，显示「课程名 + 👨‍🏫 教师」，无教师显示「未安排教师」，特殊节次午休/午餐/延时不触发）。
- 跨设备同步：用户选 GitHub Gist 方案。顶栏新增「☁ 同步」按钮（`open-sync`）+ 状态点 `#syncDot`（ok/warn/err/busy 四态）。整套 sync 模块：`SYNC` 配置存 `localStorage`(key `xifan_sync_v1`)，数据以 `{rev,ts,db}` 包成 Gist 文件 `xifan-workbench-data.json`；`gistApi()` 走 `https://api.github.com/gists` 带 `Authorization: Bearer`；支持 新建/连接/上传/下载 + 自动同步开关（`save()` 后 debounce 1.2s 自动 push）；冲突检测（远程 rev != lastRev 时标 warn 并提示先下载）；启动若已连接则自动 pull 拉取最新云端数据；`applyRemote()` 用远端 db 覆盖本地并补默认字段后重渲染当前视图。
- 使用前提：需用户自配 GitHub PAT（勾 gist 权限），多设备共用同一 token + Gist ID 即互相同步。已实现 `node --check` 通过。
